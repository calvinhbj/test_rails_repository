<!DOCTYPE html>
<html lang="zh_CN">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />

		<title><%= content_for?(:title) ? yield(:title) : "Rails Project" %></title>

		<%= stylesheet_link_tag    "application" %>
		<%= javascript_include_tag "vendor/modernizr" %>
		<%= csrf_meta_tags %>
	</head>

	<body>
		<nav class="top-bar" data-topbar>
			<ul class="title-area">
				<li class="name">
					<h1><%= link_to t(:home),welcome_path %></h1>
				</li>
			</ul>
			<section class="top-bar-section">
				<!-- Right Nav Section -->
				<ul class="right">
					<li><a href="#" style="color: yellow"><%= notice %></a></li>
					<% if user_signed_in? then %>
					<li  >
						<a href="#">当前用户:<%=current_user.email%></a>
					</li>
					<li  >
						<a href="#"><%= link_to t(:sign_out),destroy_user_session_path , method: :delete%></a>
					</li>
					<% else %>
					<li><a href="#"><%= link_to t(:sign_in),new_user_session_path  %></a></li>
					<li><a href="#"><%= link_to t(:sign_up),new_user_registration_path  %></a></li>
					<% end %>
				</ul>
				<!-- Left Nav Section -->
				<ul class="left">
					<li class="name">
						<h1><%= link_to t(:member) , members_path %></h1>
					</li>
					<li class="name">
						<h1><%= link_to t(:combo), combos_path %></h1>
					</li>
				</ul>
			</section>
		</nav>

		<div class="panel clearfix" style="padding-bottom: 100px;">
			<%= yield %>
			<%= javascript_include_tag "application" %>
		</div>
	</body>
</html>